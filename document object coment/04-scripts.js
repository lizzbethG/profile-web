const row= document.querySelectorAll('.row');
console.log(row);

const id= document.querySelectorAll('#contacto');
console.log(id);

// Si un elemento no existe

const noExiste= document.querySelectorAll('no-existe');
console.log(noExiste)